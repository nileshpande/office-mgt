import { DirectoneDirective } from './directone.directive';

describe('DirectoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectoneDirective();
    expect(directive).toBeTruthy();
  });
});
